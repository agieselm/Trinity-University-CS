#include <algotithms>
#include <vector>
#include <iostream>

using namespace std;

int weightLimit = 5000;
int maxItems  = 80000;

pair<double,vector<int>> knapsack(int weightLimit, const vector<int> &weights, const vector<double> &values) {  //first value = max value. second element = indices of which items you choose to get that value

  vector<vector<double>> table (size+1, vector<double>(weightLimit+1));
 
  auto ks = (w, i);

    for(double i = 0; i != values.size(); i++ {
      for(w = 0; w != weightLimit(); w++) {
        if(weights[i] <= w) {
          look
          
          
  
}  
