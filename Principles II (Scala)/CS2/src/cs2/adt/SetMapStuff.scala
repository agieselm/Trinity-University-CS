package cs2.ParametricSorting

/**
 * @author agieselm
 */
/*   object SetMapStuff {
  
  def setExamples() {
    val s1 = scala.collection.immutable.Set[Char]('a', 'b', 'c')
    val s2 = scala.collection.immutable.Set[Char]('b', 'c', 'q', 'd')
    println(s1)
    println(s2)
    println(s1 -- s2)
    var s3 = scala.collection.mutable.Set[Char]('r', 's', 't')
    println(s3)
    println(s1.contains('c'))
    println(s3('s'))
    println(s3.size)
  }
  
  def countWordsInFile(name:String) {
    val input = Source.fromFile(name)
    val words = new scala.collection.mutable.HashSet[String]()
    for(line <- input.getLines) {
      var linewords = line.split("\\s+")
      words ++= linewords
      
    }
    for(word <- words) println(word)
    println(words.size)
  }
  
  def main(args:Array)[String]) {
    countWordsInFile("tempest.txt")
    //setExamples
  }
}
*/