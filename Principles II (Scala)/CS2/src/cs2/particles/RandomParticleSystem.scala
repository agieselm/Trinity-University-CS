package cs2.particle
/*
import cs2.util.Vec2

/**
 * @author agieselm
 */
class RandomParticleSystem(o:Vec2) extends ParticleSystem(o) {
  override def addParticle() {
    if(math.random > 0.5) {
      super.addParticle()
    } else {
       parts ::= new RainbowParticle(new Vec2(origin), 
        new Vec2(math.random*-0.5, math.random*-0.5)
    }
    
  }
  
}
*/